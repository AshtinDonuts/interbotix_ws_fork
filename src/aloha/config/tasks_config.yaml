# ==============================================================================
# Task Configuration File (task_config.yaml)
# ==============================================================================
# This file contains configuration settings for various tasks on the Aloha robot
# systems. Each task has specific parameters, such as dataset directory and 
# episode length, tailored for different Aloha robot setups (e.g., mobile, 
# stationary, solo).
# ==============================================================================

tasks:
  # ----------------------------------------------------------------------------
  # Task: Aloha Mobile Dummy
  # ----------------------------------------------------------------------------
  aloha_mobile_dummy:
    # Path to the dataset storage directory
    dataset_dir: "~/aloha_data/aloha_mobile_dummy"  
    # Length of each episode in timesteps
    episode_len: 300                        

  # ----------------------------------------------------------------------------
  # Task: Aloha Stationary Dummy
  # ----------------------------------------------------------------------------
  aloha_stationary_dummy:
    # Path to the dataset storage directory
    dataset_dir: "~/aloha_data/aloha_stationary_dummy"  
    # Length of each episode in timesteps
    episode_len: 500                                   

  # ----------------------------------------------------------------------------
  # Task: Aloha Solo Dummy
  # ----------------------------------------------------------------------------
  aloha_solo_dummy:
    # Path to the dataset storage directory
    dataset_dir: "~/aloha_data/aloha_solo_dummy"  
    # Length of each episode in timesteps
    episode_len: 500                              


  # ----------------------------------------------------------------------------
  # Custom Defined Tasks

  aloha_solo_plug:
    dataset_dir: "~/aloha_data/aloha_solo_plug"  
    episode_len: 500

  gravity_torque_task:
    dataset_dir: "~/aloha_data/gravity_torque_task"
    episode_len: 500
  
  ## SUCCESSFUL PICK N PLACE
  picknplace_JAN8_1:
    dataset_dir: "/mnt/c2d9b23a-b03e-4fdb-82ad-59f039ec9e3e/khw/picknplace_JAN8_1"  
    episode_len: 500

  ## LEGO INSERT
  lego_insert:
    dataset_dir: "/mnt/c2d9b23a-b03e-4fdb-82ad-59f039ec9e3e/khw/lego_insert"  
    episode_len: 400
  
  lego_insert_v2:
    dataset_dir: "/mnt/c2d9b23a-b03e-4fdb-82ad-59f039ec9e3e/khw/lego_insert_v2"  
    episode_len: 400
